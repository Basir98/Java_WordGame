package word_game;

public class Main {
	
	/*
	 * 	Application description
	 * 
	 * One long array of random words which is different each time. Random word with
	 * each iteration, when the submit btn is clicked, and the user can use the hint
	 * btn to get a hint for the word. After it is done instead of string, char can
	 * be sent and the user have to wait for the threads to finish before submitting
	 * a new guess. 
	 */
	
	public static void main(String[] args) {
		DialogOption dOption = new DialogOption();
		dOption.startDialogFrame();
	}

}
